# 阶段04：事件协议与后端详情接口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务执行。步骤使用 checkbox (`- [ ]`) 语法跟踪。

**Goal:** 把真实采集运行时的状态、错误、秒级进度和质量摘要，稳定暴露到 `research_platform` 事件流与 `data_center_collection` 详情接口。

**Architecture:** 复用阶段02已经落下的 `ResearchCollectionService` listener 能力，在 `ResearchPlatformService` 做事件桥与兼容协议收口；再扩 session detail payload；最后补一条 fake runtime 的 API+WebSocket 集成测试，保证“会话变化 -> 事件推送 -> 详情可读”闭环成立。

**Tech Stack:** Python、FastAPI、现有 `app.api.websocket` 广播通道、pytest、Starlette WebSocket test helpers。

---

## File Structure

- Modify: `backend/app/core/research_platform/service.py`
- Modify: `backend/app/core/storage_research_platform.py`
- Modify: `backend/app/api/data_center_collection.py`
- Modify: `backend/app/api/websocket.py`
- Test: `backend/tests/test_research_platform_service.py`
- Test: `backend/tests/test_data_center_collection_api.py`
- Test: `backend/tests/test_research_platform_websocket.py`
- Create: `backend/tests/test_data_center_collection_api_runtime_integration.py`

### Task 1: 让 collection 事件通过 `research_platform` 服务桥接出去

**Files:**
- Modify: `backend/app/core/research_platform/service.py`
- Test: `backend/tests/test_research_platform_service.py`

- [ ] **Step 1: 写失败测试，固定细粒度采集事件向 `ResearchPlatformService` 透传**

```python
def test_research_platform_forwards_collection_runtime_events():
    seen = []

    class _FakeCensus:
        async def start(self):
            return None

        async def stop(self):
            return None

    class _FakeDataset:
        pass

    class _FakeTraining:
        pass

    class _FakeStorage:
        pass

    class FakeCollectionService:
        def add_listener(self, listener):
            self.listener = listener

    service = ResearchPlatformService(
        collection_service=FakeCollectionService(),
        census_service=_FakeCensus(),
        dataset_service=_FakeDataset(),
        training_service=_FakeTraining(),
        storage=_FakeStorage(),
    )
    service.add_listener(seen.append)
    service._collection.listener({'event': 'session_running', 'session_id': 'sess-1'})
    service._collection.listener({'event': 'second_flushed', 'session_id': 'sess-1', 'second_bucket': 1713000000})

    assert seen[0]['event'] == 'session_running'
    assert seen[0]['session_id'] == 'sess-1'
    assert seen[1]['event'] == 'second_flushed'
    assert seen[1]['second_bucket'] == 1713000000
```

- [ ] **Step 2: 运行 service 测试**

Run:

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_research_platform_service.py -q
```

Expected:

```text
FAIL
```

- [ ] **Step 3: 复用阶段02的 listener 机制，只在 platform service 中桥接**

```python
class ResearchPlatformService:
    def __init__(self, *, collection_service, census_service, dataset_service, training_service, storage):
        self._collection = collection_service
        self._census = census_service
        self._dataset = dataset_service
        self._training = training_service
        self._storage = storage
        if hasattr(self._collection, 'add_listener'):
            self._collection.add_listener(self._forward_collection_event)

    def _forward_collection_event(self, payload: dict[str, object]) -> None:
        self._emit(payload)
```

- [ ] **Step 4: 运行 service 测试**

Run:

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_research_platform_service.py -q
```

Expected:

```text
PASS
```

- [ ] **Step 5: 提交本任务**

```bash
git add backend/app/core/research_platform/service.py backend/tests/test_research_platform_service.py
git commit -m "打通采集事件桥"
```

### Task 2: 扩 session detail payload，提供进度、错误和图表摘要

**Files:**
- Modify: `backend/app/core/storage_research_platform.py`
- Modify: `backend/app/api/data_center_collection.py`
- Test: `backend/tests/test_data_center_collection_api.py`

- [ ] **Step 1: 写失败测试，固定 detail payload 的新增结构**

```python
def test_get_collection_session_returns_runtime_fields():
    module = _load_module()

    class FakeService:
        def get_collection_session_detail(self, session_id):
            return {
                'session_id': session_id,
                'status': 'running',
                'last_error_code': '',
                'progress': {
                    'written_seconds': 12,
                    'remaining_seconds': 1788,
                    'seconds_to_full_window': 7188,
                    'seconds_to_next_boundary': 543,
                },
                'coverage': {'coverage_ratio': 0.98},
                'charts': {'price': [], 'trade': [], 'book': []},
            }

    class FakeContext:
        def research_platform(self):
            return FakeService()

    detail = asyncio.run(module.get_collection_session('sess-1', ctx=FakeContext()))
    assert detail['session']['progress']['written_seconds'] == 12
```

- [ ] **Step 2: 运行 API 测试**

Run:

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_data_center_collection_api.py -q
```

Expected:

```text
FAIL
```

- [ ] **Step 3: 在 session detail 中增加 progress / runtime 字段**

```python
def get_collection_session_detail(self, session_id: str) -> dict[str, object] | None:
    session = self._storage.get_research_collection_session(session_id)
    if session is None:
        return None
    coverage = self._storage.get_research_session_coverage_summary(session_id)
    progress = self._storage.get_research_session_progress_summary(session_id)
    return {
        **session,
        'progress': progress,
        'coverage': coverage,
        'charts': self._storage.get_research_session_chart_series(session_id),
    }
```

- [ ] **Step 4: 运行 API 测试**

Run:

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_data_center_collection_api.py -q
```

Expected:

```text
PASS
```

- [ ] **Step 5: 提交本任务**

```bash
git add backend/app/core/storage_research_platform.py backend/app/api/data_center_collection.py backend/tests/test_data_center_collection_api.py
git commit -m "扩展采集详情接口"
```

### Task 3: 加一条 API + WebSocket 集成测试，锁死事件协议

**Files:**
- Modify: `backend/app/core/research_platform/service.py`
- Modify: `backend/app/api/websocket.py`
- Test: `backend/tests/test_research_platform_service.py`
- Test: `backend/tests/test_research_platform_websocket.py`
- Create: `backend/tests/test_data_center_collection_api_runtime_integration.py`

- [ ] **Step 1: 写失败测试，固定细粒度事件广播和 `session_updated` 兼容语义**

```python
def test_platform_bridge_emits_session_updated_for_session_events():
    seen = []

    class _FakeCensus:
        async def start(self):
            return None

        async def stop(self):
            return None

    class _FakeDataset:
        pass

    class _FakeTraining:
        pass

    class _FakeStorage:
        pass

    class FakeCollectionService:
        def add_listener(self, listener):
            self.listener = listener

    service = ResearchPlatformService(
        collection_service=FakeCollectionService(),
        census_service=_FakeCensus(),
        dataset_service=_FakeDataset(),
        training_service=_FakeTraining(),
        storage=_FakeStorage(),
    )
    service.add_listener(seen.append)
    service._forward_collection_event({'event': 'session_failed', 'session_id': 'sess-1'})

    assert [payload['event'] for payload in seen] == ['session_failed', 'session_updated']


def test_websocket_broadcasts_collection_runtime_events(monkeypatch):
    ctx = _DummyContext()
    manager = websocket_api.ConnectionManager()
    websocket = _DummyWebSocket()

    monkeypatch.setattr(websocket_api, 'get_ctx', lambda: ctx)
    asyncio.run(manager.connect(websocket))
    asyncio.run(manager.handle_subscribe(websocket, {'action': 'subscribe', 'channels': ['research_platform']}))
    asyncio.run(manager._broadcast_research_platform({'event': 'session_failed', 'session_id': 'sess-1'}))
    asyncio.run(manager._broadcast_research_platform({'event': 'second_flushed', 'session_id': 'sess-1', 'second_bucket': 1713000000}))
    asyncio.run(manager._broadcast_research_platform({'event': 'session_quality_updated', 'session_id': 'sess-1'}))

    assert 'session_failed' in websocket.sent_messages[0]
    assert 'second_flushed' in websocket.sent_messages[1]
    assert 'session_quality_updated' in websocket.sent_messages[2]
```

- [ ] **Step 2: 运行 websocket / integration 测试**

Run:

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_research_platform_service.py tests/test_research_platform_websocket.py tests/test_data_center_collection_api_runtime_integration.py -q
```

Expected:

```text
FAIL
```

- [ ] **Step 3: 在 platform bridge 中保留细事件，并对所有 `session_*` 追加兼容 `session_updated`**

```python
def _forward_collection_event(self, payload: dict[str, object]) -> None:
    self._emit(payload)
    event_name = str(payload.get('event', ''))
    if event_name.startswith('session_'):
        self._emit({**payload, 'event': 'session_updated'})
```

- [ ] **Step 4: 运行 websocket / integration 测试**

Run:

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_research_platform_service.py tests/test_research_platform_websocket.py tests/test_data_center_collection_api_runtime_integration.py -q
```

Expected:

```text
PASS
```

- [ ] **Step 5: 提交本任务**

```bash
git add backend/app/core/research_platform/service.py backend/app/api/websocket.py backend/tests/test_research_platform_service.py backend/tests/test_research_platform_websocket.py backend/tests/test_data_center_collection_api_runtime_integration.py
git commit -m "锁定采集运行时事件协议"
```
