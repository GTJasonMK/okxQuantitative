# 阶段02：会话状态与采集运行时骨架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务执行。步骤使用 checkbox (`- [ ]`) 语法跟踪。

**Goal:** 为真实秒级采集建立后端会话状态协议、错误字段和可注入运行时骨架，同时保持现有 `research_platform` / `data_center_collection` API 入口不变。

**Architecture:** 先把 `research_collection_sessions` 升级为显式状态机对象，再引入 `CollectionRuntimeRegistry` 和 `CollectionSessionController`。`ResearchCollectionService` 退化为 facade，只负责把 API 动作委托给 controller。

**Tech Stack:** Python、FastAPI 现有 service/factory、SQLite storage mixin、pytest、pytest-asyncio。

---

## File Structure
- Create: `backend/app/core/data_center_collection/__init__.py`, `backend/app/core/data_center_collection/runtime_registry.py`, `backend/app/core/data_center_collection/controller.py`
- Modify: `backend/app/core/storage_research_platform_schema.py`, `backend/app/core/storage_research_platform.py`, `backend/app/core/research_platform/collection/service.py`, `backend/app/core/research_platform/factory.py`
- Test: `backend/tests/test_research_platform_storage.py`, `backend/tests/test_research_platform_collection_runtime.py`, `backend/tests/test_data_center_collection_controller.py`, `backend/tests/test_data_center_collection_runtime_registry.py`

### Task 1: 扩展 session schema，冻结会话状态机字段

**Files:**
- Modify: `backend/app/core/storage_research_platform_schema.py`
- Modify: `backend/app/core/storage_research_platform.py`
- Test: `backend/tests/test_research_platform_storage.py`

- [ ] **Step 1: 先写失败测试，固定新 session 字段和默认状态**

```python
def test_storage_round_trips_collection_session(storage):
    session_id = storage.create_research_collection_session(
        inst_id='BTC-USDT-SWAP',
        planned_duration_sec=3600,
        trigger_mode='manual',
        trigger_note='operator-started',
        sampling_policy_id='manual_session_v1',
        integrity_policy_version='strict_v1',
        collector_version='research_collector_v1',
        source_config_hash='cfg-1',
        feature_recipe_version='second_state_causal_tensor_v1',
        book_channel='books5',
    )
    row = storage.get_research_collection_session(session_id)
    assert row['status'] == 'starting'
    assert row['stop_reason'] == ''
    assert row['last_error_code'] == ''
    assert row['last_error_message'] == ''
    assert row['failed_at'] is None
```

- [ ] **Step 2: 运行测试，确认当前实现仍是旧协议**

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_research_platform_storage.py::test_storage_round_trips_collection_session -q
```

Expected: `FAIL`

- [ ] **Step 3: 实现 session columns 和状态常量**

```python
# backend/app/core/storage_research_platform_schema.py
SESSION_COLUMNS = (
    'session_id', 'inst_id', 'started_at', 'ended_at', 'planned_duration_sec',
    'status', 'stop_reason', 'last_error_code', 'last_error_message', 'failed_at',
    'collector_version', 'source_config_hash', 'trigger_mode', 'trigger_note',
    'sampling_policy_id', 'integrity_policy_version', 'feature_recipe_version',
    'book_channel', 'valid_second_count', 'missing_second_count',
    'book_stale_second_count', 'state_stale_second_count', 'coverage_ratio', 'quality_score',
)
```

```python
# backend/app/core/storage_research_platform.py
SESSION_STATUS_STARTING = 'starting'
SESSION_STATUS_RUNNING = 'running'
SESSION_STATUS_STOPPING = 'stopping'
SESSION_STATUS_STOPPED = 'stopped'
SESSION_STATUS_FINISHED = 'finished'
SESSION_STATUS_FAILED = 'failed'
row = {'status': SESSION_STATUS_STARTING, 'stop_reason': '', 'last_error_code': '', 'last_error_message': '', 'failed_at': None}
```

- [ ] **Step 4: 运行 storage 测试**

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_research_platform_storage.py -q
```

Expected: `PASS`

- [ ] **Step 5: 提交本任务**

```bash
git add backend/app/core/storage_research_platform_schema.py backend/app/core/storage_research_platform.py backend/tests/test_research_platform_storage.py
git commit -m "收紧采集会话状态协议"
```

### Task 2: 引入运行时注册表，明确活动会话唯一性

**Files:**
- Create: `backend/app/core/data_center_collection/runtime_registry.py`
- Create: `backend/app/core/data_center_collection/__init__.py`
- Test: `backend/tests/test_data_center_collection_runtime_registry.py`

- [ ] **Step 1: 先写失败测试，固定 registry 行为**

```python
import pytest

from app.core.data_center_collection.runtime_registry import CollectionRuntimeRegistry


class _DummyRunner:
    async def start(self):
        return None
    async def stop(self):
        return None


def test_runtime_registry_rejects_multiple_active_sessions():
    registry = CollectionRuntimeRegistry()
    registry.register('sess-1', _DummyRunner())
    with pytest.raises(ValueError, match='active collection session already exists'):
        registry.register('sess-2', _DummyRunner())


def test_runtime_registry_unregisters_runner():
    registry = CollectionRuntimeRegistry()
    registry.register('sess-1', _DummyRunner())
    registry.unregister('sess-1')
    assert registry.get('sess-1') is None
```

- [ ] **Step 2: 运行测试，确认 registry 尚不存在**

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_data_center_collection_runtime_registry.py -q
```

Expected: `FAIL`

- [ ] **Step 3: 实现最小 registry**

```python
class CollectionRuntimeRegistry:
    def __init__(self):
        self._runtimes: dict[str, object] = {}

    def register(self, session_id: str, runtime: object) -> None:
        if self.active_session_id() is not None:
            raise ValueError('active collection session already exists')
        self._runtimes[session_id] = runtime

    def get(self, session_id: str) -> object | None:
        return self._runtimes.get(session_id)

    def unregister(self, session_id: str) -> None:
        self._runtimes.pop(session_id, None)

    def active_session_id(self) -> str | None:
        return next(iter(self._runtimes), None)
```

- [ ] **Step 4: 运行 registry 测试**

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_data_center_collection_runtime_registry.py -q
```

Expected: `2 passed`

- [ ] **Step 5: 提交本任务**

```bash
git add backend/app/core/data_center_collection/__init__.py backend/app/core/data_center_collection/runtime_registry.py backend/tests/test_data_center_collection_runtime_registry.py
git commit -m "增加采集运行时注册表"
```

### Task 3: 增加 controller 骨架、listener 机制，并让 `ResearchCollectionService` 变成 facade

**Files:**
- Create: `backend/app/core/data_center_collection/controller.py`
- Modify: `backend/app/core/research_platform/collection/service.py`
- Modify: `backend/app/core/research_platform/factory.py`
- Test: `backend/tests/test_data_center_collection_controller.py`
- Test: `backend/tests/test_research_platform_collection_runtime.py`

- [ ] **Step 1: 先写失败测试，固定 controller 的 start/stop、事件透传和返回语义**

```python
import asyncio

from app.core.data_center_collection.controller import CollectionSessionController
from app.core.data_center_collection.runtime_registry import CollectionRuntimeRegistry
from app.core.storage_research_platform import SESSION_STATUS_RUNNING
from app.core.storage_research_platform import SESSION_STATUS_STOPPED


class _DummyRuntime:
    def __init__(self, *, storage, emit_event, session_id):
        self._storage = storage
        self._emit_event = emit_event
        self._session_id = session_id
        self.started = False
        self.stopped = False
        self.stop_reason = ''

    async def start(self):
        self.started = True
        self._emit_event({'event': 'session_started', 'session_id': self._session_id})
        self._storage.update_research_collection_session(
            self._session_id,
            status=SESSION_STATUS_RUNNING,
        )

    async def stop(self, *, stop_reason):
        self.stopped = True
        self.stop_reason = stop_reason
        self._storage.update_research_collection_session(
            self._session_id,
            status=SESSION_STATUS_STOPPED,
            stop_reason=stop_reason,
        )
        self._emit_event({
            'event': 'session_stopped',
            'session_id': self._session_id,
            'stop_reason': stop_reason,
        })


def _payload():
    return {
        'inst_id': 'BTC-USDT-SWAP',
        'planned_duration_sec': 1800,
        'trigger_mode': 'manual',
        'trigger_note': '',
        'sampling_policy_id': 'manual_session_v1',
        'integrity_policy_version': 'strict_v1',
        'collector_version': 'research_collector_v1',
        'source_config_hash': 'manual',
        'feature_recipe_version': 'second_state_causal_tensor_v1',
        'book_channel': 'books5',
    }


def test_controller_returns_latest_session_after_runtime_start(storage):
    seen = []
    controller = CollectionSessionController(
        storage=storage,
        runtime_registry=CollectionRuntimeRegistry(),
        runtime_factory=lambda session, emit_event: _DummyRuntime(
            storage=storage,
            emit_event=emit_event,
            session_id=session['session_id'],
        ),
    )
    session = asyncio.run(controller.start_session(_payload(), emit_event=seen.append))
    assert session['status'] == SESSION_STATUS_RUNNING
    assert seen[0]['event'] == 'session_started'


def test_controller_marks_session_stopping_then_returns_stopped_session(storage):
    seen = []
    controller = CollectionSessionController(
        storage=storage,
        runtime_registry=CollectionRuntimeRegistry(),
        runtime_factory=lambda session, emit_event: _DummyRuntime(
            storage=storage,
            emit_event=emit_event,
            session_id=session['session_id'],
        ),
    )
    session = asyncio.run(controller.start_session(_payload(), emit_event=seen.append))
    stopped = asyncio.run(
        controller.stop_session(
            session['session_id'],
            stop_reason='manual_stop',
            emit_event=seen.append,
        )
    )
    assert stopped['status'] == SESSION_STATUS_STOPPED
    assert stopped['stop_reason'] == 'manual_stop'
    assert seen[-1]['event'] == 'session_stopped'
```

- [ ] **Step 2: 运行 controller 测试**

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_data_center_collection_controller.py -q
```

Expected: `FAIL`

- [ ] **Step 3: 实现 controller，并重构 facade service**

```python
class CollectionSessionController:
    def __init__(self, *, storage, runtime_registry, runtime_factory):
        self._storage = storage
        self._runtime_registry = runtime_registry
        self._runtime_factory = runtime_factory

    async def start_session(self, payload: dict[str, object], *, emit_event) -> dict[str, object]:
        session_id = self._storage.create_research_collection_session(**payload)
        session = self._storage.get_research_collection_session(session_id)
        runtime = self._runtime_factory(session, emit_event)
        self._runtime_registry.register(session_id, runtime)
        try:
            await runtime.start()
        except Exception:
            self._runtime_registry.unregister(session_id)
            raise
        return self._storage.get_research_collection_session(session_id)

    async def stop_session(
        self,
        session_id: str,
        *,
        stop_reason: str,
        emit_event,
    ) -> dict[str, object]:
        runtime = self._runtime_registry.get(session_id)
        if runtime is None:
            raise ValueError('collection runtime not found')
        self._storage.update_research_collection_session(
            session_id,
            status='stopping',
            stop_reason=stop_reason,
        )
        emit_event({
            'event': 'session_stopping',
            'session_id': session_id,
            'stop_reason': stop_reason,
        })
        try:
            await runtime.stop(stop_reason=stop_reason)
        finally:
            self._runtime_registry.unregister(session_id)
        return self._storage.get_research_collection_session(session_id)
```

```python
class ResearchCollectionService:
    def __init__(self, *, storage, controller):
        self._storage = storage
        self._controller = controller
        self._listeners: list[Callable[[dict[str, object]], None]] = []

    def add_listener(self, listener):
        if listener not in self._listeners:
            self._listeners.append(listener)

    def remove_listener(self, listener):
        if listener in self._listeners:
            self._listeners.remove(listener)

    def emit_event(self, payload: dict[str, object]) -> None:
        for listener in list(self._listeners):
            listener(payload)

    async def start_collection_session(self, payload):
        return await self._controller.start_session(payload, emit_event=self.emit_event)

    async def stop_collection_session(self, session_id, *, stop_reason='manual_stop'):
        return await self._controller.stop_session(
            session_id,
            stop_reason=stop_reason,
            emit_event=self.emit_event,
        )
```

- [ ] **Step 4: 在 factory 中注入 bootstrap runtime，并固定新签名**

```python
class _BootstrapRuntime:
    def __init__(self, *, storage, session, emit_event):
        self._storage = storage
        self._session = session
        self._emit_event = emit_event

    async def start(self):
        self._emit_event({
            'event': 'session_started',
            'session_id': self._session['session_id'],
        })

    async def stop(self, *, stop_reason):
        self._storage.update_research_collection_session(
            self._session['session_id'],
            status='stopped',
            stop_reason=stop_reason,
        )
        self._emit_event({
            'event': 'session_stopped',
            'session_id': self._session['session_id'],
            'stop_reason': stop_reason,
        })


storage = ctx.storage()
registry = CollectionRuntimeRegistry()
controller = CollectionSessionController(
    storage=storage,
    runtime_registry=registry,
    runtime_factory=lambda session, emit_event: _BootstrapRuntime(
        storage=storage,
        session=session,
        emit_event=emit_event,
    ),
)
collection_service = ResearchCollectionService(storage=storage, controller=controller)
```

- [ ] **Step 5: 运行 controller + collection service 测试**

```bash
cd /mnt/e/Code/okxQuantitative/backend && timeout 60s uv run pytest tests/test_data_center_collection_controller.py tests/test_research_platform_collection_runtime.py -q
```

Expected: `PASS`

- [ ] **Step 6: 提交本任务**

```bash
git add backend/app/core/data_center_collection/controller.py backend/app/core/research_platform/collection/service.py backend/app/core/research_platform/factory.py backend/tests/test_data_center_collection_controller.py backend/tests/test_research_platform_collection_runtime.py
git commit -m "接入采集会话控制器骨架"
```
